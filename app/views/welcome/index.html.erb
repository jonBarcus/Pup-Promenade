<!-- <div class="small-12 large-6 columns"> -->

  <!-- <div class ="row"> -->

  <!-- </div> -->
  <% if walker_logged_in? %>
    <h4>Dog Walker's Profile</h4>

    <a href="/walkers/profile">Configure Profile</a>

  <a href="/walkers/profile">Configure Profile</a>


  <%= render 'walker_prof_info' %>

  <% elsif owner_logged_in? %>

  <ul>
    <p>
      <h4>Owner's Profile</h4> <a href="/dogs/new">Add your pup!</a>
    </p>
    <p>
      <a href = "/search">Search for a Walker</a>
    </p>
    <p>
      <a href ="/owners/profile">Configure Your Profile</a>
    </p>
  </ul>

  <%= render 'owner_prof_info' %>



  <ul>
    <h2><small>Your Puppies!</small></h2>
  </ul>
  <ul>
    <% @dogs.each do |dog| %>
      <li><%=link_to(dog.name, controller: :dogs, action: :show, id: dog.id) %></li>
    <% end %>
  </ul>

  <% else %>

  <% end %>
<!-- </div> -->

<div class="fixed">
  <nav class="top-bar" data-topbar>
    <section class="top-bar-section">
      <ul class="title-area">
        <li class="name">
          <h1><a href="/">pupPromenade</a></h1>
      </li>
      </ul>
      <ul class="right">
        <% if walker_logged_in? || owner_logged_in? %>
        <li class="active">
          <a href="logout">Logout</a>
        </li>
        <% else %>

        <li class="has-dropdown">
          <a>Sign Up</a>
          <ul class="dropdown">
            <li>
              <a href="owners/new">as a Dog Owner</a>
            </li>
            <li>
              <a href="walkers/new">as a Dog Walker</a>
            </li>
          </ul>
        </li>
        <li class="active">
          <a href="/session/new">Login</a>
        </li>
      </ul>

    </section>
  </nav>
</div>

<div id="wrapper">
  <div id="title-text">
    <h3 id="welcome-text">Welcome to</h3>
  </div>
  <div id="title-div">
    <h1 id="welcome-title">pupPromenade</h1>
  </div>
  <p id="slogan">Find a walker for your tail-wagger</p>
</div>
<% end %>
