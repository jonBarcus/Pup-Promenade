<!-- need a form neighborhoods

< %= profile.collection_select(:neighborhood_id, @neighborhoods, :id, :name) %> -->

<!-- need a form for dogs -->

<!-- need a form for skills -->


<%= form_for @walker do |f| %>
  <%= f.label(:image_url) %>
  <%= f.text_field(:image_url) %>
  <br>
    <%= f.fields_for :walker_profile do |profile| %>
      <%= profile.label(:neighborhood_ids) %>
      <% binding.pry %>
      <%= profile.collection_select(:neighborhood_ids, @neighborhoods, :id, :name, {}, {multiple: :true}) %>
  <% end %>
  <br>
  <%= f.label(:bio) %>
  <%= f.text_area(:bio) %>
  <%= f.submit %>
<% end %>
